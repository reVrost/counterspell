version: "2"
run:
  tests: false

linters:
  # Start simple: only "critical" or high signal linters
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
  default: fast
